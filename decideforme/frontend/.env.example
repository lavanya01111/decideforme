## Frontend environment variables (Vercel)
#
# In production, point this to your Render backend base URL (WITHOUT /api).
# Example:
# VITE_API_URL=https://decideforme-backend.onrender.com
#
# In local development, you can omit this because Vite proxies /api to localhost:5000.
VITE_API_URL=

